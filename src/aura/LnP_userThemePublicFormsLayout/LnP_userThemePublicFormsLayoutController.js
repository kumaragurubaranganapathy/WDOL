({
	init : function(component, event, helper) {
        helper.checkForBrowser(component,event);
		//helper.showNavigation(component, event, helper);
        helper.setBackground(component, event, helper);
        
	}
})