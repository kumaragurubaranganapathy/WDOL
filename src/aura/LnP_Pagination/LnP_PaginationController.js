({
    doInit : function(component, event, helper) {
        helper.doInit(component, event);
    },
    showPrev : function(component, event, helper) {
        helper.showPrev(component, event);
    },
    showNo : function(component, event, helper) {
        helper.showNo(component, event);
    },
    showNext : function(component, event, helper) {
        helper.showNext(component, event);
    }
})